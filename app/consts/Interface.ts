export type urlInterface = {
    date: string,
    description: string,
    image: string,
    tags: string[],
    presets: string[],
    title: string,
    url: string
}

export type listInterface = {
    image: string,
    name: string,
    type: string
}

